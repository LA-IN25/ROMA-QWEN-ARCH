# Inside ~/SentientResearchAgent/.env

# =======================================================================
# 1. LLM Provider Keys: ROUTE ALL TRAFFIC TO LOCAL LM STUDIO (QWEN)
#    We use OPENAI_API_BASE because LM Studio uses the OpenAI compatibility layer.
# =======================================================================

# 🟢 MANDATORY CHECK SATISFACTION: OpenRouter is required by some core agents (like the
#    one that failed: OpenAICustomSearcher). We provide a placeholder value.
OPENROUTER_API_KEY="placeholder_for_agent_check"

# 🟢 LOCAL LLM CONFIGURATION (LiteLLM uses these variables)
#    This directs all LLM calls (e.g., Qwen) to your local LM Studio server.
#    The API Key is a required placeholder for the OpenAI client library.
OPENAI_API_KEY="lm-studio"
OPENAI_API_BASE="http://localhost:1234/v1" 

# Optional keys are empty as we are using the local server:
GOOGLE_GENAI_API_KEY=
ANTHROPIC_API_KEY=

# =======================================================================
# 2. Search & Tool Keys: DISABLE ALL OPTIONAL CLOUD DEPENDENCIES
# =======================================================================

# ❌ DISABLE EXA (It requires a paid key and is redundant with SearXNG)
EXA_API_KEY=

# ❌ DISABLE E2B (This was causing failures in CryptoMarketAnalyzer)
E2B_API_KEY=

# Set these to empty strings or comments to ensure toolkits fail fast and skip loading
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=

# Ensure optional features are explicitly disabled:
S3_MOUNT_ENABLED=false
