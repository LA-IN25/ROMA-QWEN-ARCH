# SentientResearchAgent Environment Configuration
# Copy this file to .env and fill in the necessary values.

# ===== Local LLM & Search Configuration =====
# These settings configure the agent to use your local LM Studio and SearXNG.

# The model identifier used in agents.yaml's `${LOCAL_MODEL_ID}` placeholder.
# IMPORTANT: Include the "openai/" prefix so litellm routes it correctly.
LOCAL_MODEL_ID=openai/openai-gpt-oss-20b-abliterated-uncensored-neo-imatrix

# The API base URL for your LM Studio server (or other OpenAI-compatible server).
OPENAI_API_BASE_URL=http://localhost:1234/v1

# API Key for the local server. LM Studio doesn't usually require one,
# but litellm does. Set this to any non-empty string.
OPENAI_API_KEY=lm-studio

# The base URL for your local SearXNG instance.
SEARXNG_URL=http://localhost:8080

# ===== Cloud LLM Provider Keys (Optional - Commented out for local setup) =====
# These are NOT needed if you are only using the local model setup above.
# You can uncomment and fill these if you want to use cloud models via litellm.

# OpenRouter API key (alternative LLM provider)
# OPENROUTER_API_KEY=your_openrouter_key_here

# OpenAI API key (for direct OpenAI usage, distinct from the local key above)
# OPENAI_API_KEY=your_openai_key_here

# Google GenAI API key (for Gemini models)
# GOOGLE_GENAI_API_KEY=your_google_genai_key_here

# Anthropic API key (for Claude models)
# ANTHROPIC_API_KEY=your_anthropic_key_here


# ===== Search & Tool Keys (Optional) =====
# These are only needed if you enable agents/toolkits that use these services.

# Exa API key (for the ExaCustomSearcher agent)
EXA_API_KEY=

# ===== E2B Code Execution & S3 Integration (Optional) =====
# These are only needed for the E2BTools toolkit (e.g., in CryptoMarketAnalyzer).

# E2B API key for sandbox code execution
E2B_API_KEY=

# E2B template ID (custom template with S3 integration)
# E2B_TEMPLATE_ID=sentient-e2b-s3

# E2B sandbox timeout in seconds (default: 300)
# E2B_TIMEOUT=300

# AWS credentials for S3 integration in E2B sandbox
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1

# S3 bucket name for data persistence and sync
# S3_BUCKET_NAME=


# ===== S3 Mounting Configuration (Optional) =====
# Used if you configure S3 mounting for data persistence across environments.

# Enable S3 mounting across environments (true/false)
# S3_MOUNT_ENABLED=false

# Universal mount directory path (identical across all platforms)
# S3_MOUNT_DIR=/opt/sentient


# ===== Server Configuration (Optional) =====
# Flask server host (default: 0.0.0.0)
# FLASK_HOST=0.0.0.0

# Flask server port (default: 5000)
# FLASK_PORT=5000


# ===== Logging Configuration (Optional) =====
# Log level (TRACE, DEBUG, INFO, SUCCESS, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Log file mode: "w" to truncate on startup, "a" to append to existing file
# LOG_FILE_MODE=w


# ===== Cache Configuration (Optional) =====
# Cache directory path (default: .agent_cache)
# CACHE_DIR=.agent_cache


# ===== Experiment Configuration (Optional) =====
# Base directory for experiment outputs (default: experiments)
# EXPERIMENTS_DIR=experiments

# Number of days to keep experiment results (default: 30)
# EXPERIMENT_RETENTION_DAYS=30

